require 'rubygems'
require 'bundler'

desc 'Generate the documentation for Splash!'
task :docs do
  sh 'rocco js/**/*.coffee --output=docs/'
end

desc 'Start watching the coffeescript directory for changes to compile'
task :watch do
  sh 'ruby C:\Ruby193\lib\ruby\gems\1.9.1\bin;\bin\guard'
end